package dev.ftb.mods.ftbic.screen;

import net.minecraft.world.Container;

public class ChargeSlot extends BatterySlot {
	public ChargeSlot(Container container, int index, int x, int y) {
		super(container, index, x, y);
	}
}
